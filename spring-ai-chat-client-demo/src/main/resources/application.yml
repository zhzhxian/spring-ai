spring:
  application:
    name: spring-ai-chat-client-demo
  ai:
    deepseek:
      api-key: deepseek的API_KEY
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat

server:
  port: 8080